/*
 * @prettier
 */
import React from 'react';

const textSize = 100;

const TakePlaceActionBar = (props) => {
    return (
        <div style={{ fontSize: textSize + 'px', textAlign: 'center' }}>
            <span>🍧</span>
            <span>🌶</span>
        </div>
    );
};

export default TakePlaceActionBar;
